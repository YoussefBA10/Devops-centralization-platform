input {
  # Beats protocol from Filebeat (default port used by your agents)
  beats {
    port => 5045
    tags => ["filebeat"]
  }

  # Optional: Direct TCP JSON input for legacy apps or direct log shipping
  tcp {
    port => 5044
    codec => json
    tags => ["direct-json"]
  }

  # Native HTTP poller for Loki (cleaner, no curl dependency, supports relative time)
  http_poller {
    urls => {
      loki_query => "http://172.17.5.190:3100/loki/api/v1/query_range?query=%7Bjob%3D~%22.%2B%22%7D&limit=5000&start=-5m"
    }
    request_timeout => 60
    schedule => { "every" => "30s" }
    codec => "json"
    metadata_target => "http_poller_metadata"
    type => "loki"
  }
}


filter {
  # Task 1: Split Loki response into individual events using Ruby
  if [type] == "loki" {
    ruby {
      code => "
        begin
          data = event.get('data')
          if data && data['result']
            data['result'].each do |result|
              labels = result['stream'] || {}
              result['values'].each do |val|
                # Loki values are [nanoseconds_string, message_string]
                new_event = LogStash::Event.new
                ts_ns = val[0].to_i
                
                # Convert nanoseconds to Logstash Timestamp
                new_event.set('@timestamp', LogStash::Timestamp.at(ts_ns / 1000000000, (ts_ns % 1000000000) / 1000000))
                new_event.set('message', val[1])
                
                # Add all Loki labels as top-level fields
                labels.each { |k, v| new_event.set(k, v) }
                
                new_event.set('type', 'loki_parsed')
                new_event.set('ingest_source', 'loki')
                
                new_event_block.call(new_event)
              end
            end
          end
          event.cancel() # Drop the original poller response event
        rescue => e
          event.tag('_rubyexception')
          event.set('ruby_error', e.message)
        end
      "
    }
    mutate {
      remove_field => ["data", "status"]
    }
  }

  # Task 1 (Deduplication Fix): Generate a unique deterministic ID for each log
  if [type] == "loki_parsed" {
    fingerprint {
      source => ["message", "@timestamp"]
      target => "[@metadata][fingerprint]"
      method => "SHA1"
      key => "monetique"
    }
  }


  # 1. Handle Filebeat Docker metadata & ECS host clash
  if [host] and ![host][name] {
    mutate {
      rename => { "host" => "host_ip" }
    }
  }

  if [container] or [docker] {
    mutate {
      add_field => {
        "service" => "%{[container][name]}"
        "service_name" => "%{[container][name]}"
      }
    }
    # Prefer explicit labels if available
    if [docker][labels][com.monetique.service] {
      mutate {
        replace => { "service" => "%{[docker][labels][com.monetique.service]}" }
        replace => { "service_name" => "%{[docker][labels][com.monetique.service]}" }
      }
    } else if [container][labels][com.monetique.service] {
      mutate {
        replace => { "service" => "%{[container][labels][com.monetique.service]}" }
        replace => { "service_name" => "%{[container][labels][com.monetique.service]}" }
      }
    }

    # Normalize service name (remove leading slash if present)
    mutate {
      gsub => [
        "service", "^/", "",
        "service_name", "^/", ""
      ]
    }
  }

  # Add service_name alias if not present (for Grafana/General consistency)
  if ![service_name] and [service] {
    mutate { add_field => { "service_name" => "%{service}" } }
  } else if ![service] and [service_name] {
    mutate { add_field => { "service" => "%{service_name}" } }
  }

  # Ensure service name is lowercase for term query compatibility
  if [service] {
    mutate {
      lowercase => ["service", "service_name"]
    }
  }

  # 2. Normalize common fields
  mutate {
    rename => {
      "[message]" => "raw_message"
      "[log][file][path]" => "log_path"
    }
    strip => ["raw_message"]
  }

  # 3. Extract Application / Environment from labels or fields (if not already set by Filebeat)
  if ![environment] {
    if [docker][labels][com.monetique.environment] {
      mutate {
        add_field => { "environment" => "%{[docker][labels][com.monetique.environment]}" }
      }
    } else if [container][labels][env] {
      mutate {
        add_field => { "environment" => "%{[container][labels][env]}" }
      }
    }
  }

  if ![node] {
    if [docker][labels][node] {
      mutate { add_field => { "node" => "%{[docker][labels][node]}" } }
    } else if [container][labels][node] {
      mutate { add_field => { "node" => "%{[container][labels][node]}" } }
    } else if [container][id] {
      mutate { add_field => { "node" => "%{[container][id]}" } }
    }
  }
  
  if ![environment] {
    mutate {
      add_field => { "environment" => "unknown" }
    }
  }

  # Ensure environment is lowercase for index naming
  mutate {
    lowercase => ["environment"]
  }
  # Try to parse raw_message as JSON first (common for Express/Node.js apps)
  if [raw_message] =~ /^\{.*\}$/ {
    json {
      source => "raw_message"
      target => "parsed_json"
    }
    if [parsed_json] {
       mutate {
         rename => { "[parsed_json][level]" => "level" }
         rename => { "[parsed_json][message]" => "message" }
         rename => { "[parsed_json][timestamp]" => "timestamp" }
         add_tag => ["json_parsed"]
       }
    }
  }

  if !("json_parsed" in [tags]) {
    grok {
      match => {
        "raw_message" => [
          "%{TIMESTAMP_ISO8601:timestamp} %{LOGLEVEL:level} %{GREEDYDATA:message}",
          "%{LOGLEVEL:level}%{SPACE}: %{GREEDYDATA:message}",
          "%{GREEDYDATA:message}"
        ]
      }
      tag_on_failure => ["_grok_message_fail"]
    }
  }

  # 5. Advanced error classification
  if [message] =~ /(?i)(exception|error|fail|timeout|connection refused)/ {
    mutate {
      add_field => { "severity" => "ERROR" }
    }
  } else if [message] =~ /(?i)(warn|warning)/ {
    mutate {
      add_field => { "severity" => "WARN" }
    }
  } else {
    mutate {
      add_field => { "severity" => "INFO" }
    }
  }

  # 6. Categorization of logs
  if [message] =~ /(?i)(database|sql|jdbc|mysql|postgres)/ {
    mutate { add_field => { "category" => "DATABASE" } }
  } else if [message] =~ /(?i)(http|rest|api|network|timeout|connection)/ {
    mutate { add_field => { "category" => "NETWORK" } }
  } else if [message] =~ /(?i)(external|third.party|oauth|api.gateway)/ {
    mutate { add_field => { "category" => "EXTERNAL_SERVICE" } }
  } else {
    mutate { add_field => { "category" => "APPLICATION" } }
  }

  # 7. Create human-readable normalized summary (for AI and UI)
  mutate {
    add_field => {
      "normalized_summary" => "%{category} - %{severity}: %{message}"
    }
  }

  # 8. Clean up unnecessary fields to save storage
  mutate {
    remove_field => ["beat", "prospector", "input", "offset", "log", "@version"]
  }

  # 9. Add timestamp if missing
  date {
    match => ["timestamp", "ISO8601"]
    target => "@timestamp"
  }
}

output {
  if [type] == "loki_parsed" {
    # Task 1: Route Loki logs to a specific daily index with deduplication
    elasticsearch {
      hosts => ["elasticsearch:9200"]
      index => "loki-logs-%{+YYYY.MM.dd}"
      document_id => "%{[@metadata][fingerprint]}"
      document_type => "_doc"

      user => "${ELASTIC_USER:elastic}"
      password => "${ELASTIC_PASSWORD:changeme}"
      manage_template => false
    }
  } else {
    # Main output: Elasticsearch with daily indices per environment
    elasticsearch {
      hosts => ["elasticsearch:9200"]
      index => "app-logs-%{[environment]}-%{+YYYY.MM.dd}"
      document_type => "_doc"
      user => "${ELASTIC_USER:elastic}"
      password => "${ELASTIC_PASSWORD:changeme}"
      manage_template => false
    }
  }
}
